//
// Created by Matthias Hofstätter on 04.04.23.
//

#include "Utils.h"

namespace utils {

/*    static IpAndPort splitIPandPort(std::string ipandport) {
        int end = ipandport.find(':');
        return {ipandport.substr(0, end), std::stoi(ipandport.substr(end, ipandport.length() - end))};
    }*/

} // mpp::utils